server:
  port: 8383
spring:
  application:
    name: dossier

  kafka:
    consumer:
      client-id: my-consumer-group
      group-id: dossier-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      auto-offset-reset: earliest
      enable-auto-commit: false
      bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:192.168.1.127:9092}
    topic:
      finish-registration: finish-registration
      create-documents: create-documents
      send-documents: send-documents
      send-ses: send-ses
      credit-issued: credit-issued
      application-denied: application-denied

logging:
  level:
    org:
      springframework:
        web:
          WARN

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
  model-and-view-allowed: false
